jdk:
  - openjdk17
before_install:
  - sdk install java 25-open
  - sdk use java 25-open
  - export JAVA_HOME="$SDKMAN_CANDIDATES_DIR/java/current"
  - java -version
install:
  - ./gradlew publishToMavenLocal -Pgroup=$GROUP -Pversion=$VERSION --no-daemon --info --console=plain
